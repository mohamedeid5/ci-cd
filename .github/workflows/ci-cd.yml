name: First Workflow

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        uses: actions/checkout@v4
      - name: run multiple commands
        run: |
          ls
          pwd
          ls -la
